package xinyiyun.chenfei.com.movecar.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import xinyiyun.chenfei.com.movecar.category.TeamInfo;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TEAM_INFO".
*/
public class TeamInfoDao extends AbstractDao<TeamInfo, Void> {

    public static final String TABLENAME = "TEAM_INFO";

    /**
     * Properties of entity TeamInfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property TeamNo = new Property(0, String.class, "TeamNo", false, "TEAM_NO");
        public final static Property Companyid = new Property(1, int.class, "companyid", false, "COMPANYID");
        public final static Property Des = new Property(2, String.class, "Des", false, "DES");
        public final static Property Total = new Property(3, Integer.class, "Total", false, "TOTAL");
        public final static Property State = new Property(4, Integer.class, "State", false, "STATE");
    }


    public TeamInfoDao(DaoConfig config) {
        super(config);
    }
    
    public TeamInfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TEAM_INFO\" (" + //
                "\"TEAM_NO\" TEXT," + // 0: TeamNo
                "\"COMPANYID\" INTEGER NOT NULL ," + // 1: companyid
                "\"DES\" TEXT," + // 2: Des
                "\"TOTAL\" INTEGER," + // 3: Total
                "\"STATE\" INTEGER);"); // 4: State
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TEAM_INFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TeamInfo entity) {
        stmt.clearBindings();
 
        String TeamNo = entity.getTeamNo();
        if (TeamNo != null) {
            stmt.bindString(1, TeamNo);
        }
        stmt.bindLong(2, entity.getCompanyid());
 
        String Des = entity.getDes();
        if (Des != null) {
            stmt.bindString(3, Des);
        }
 
        Integer Total = entity.getTotal();
        if (Total != null) {
            stmt.bindLong(4, Total);
        }
 
        Integer State = entity.getState();
        if (State != null) {
            stmt.bindLong(5, State);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TeamInfo entity) {
        stmt.clearBindings();
 
        String TeamNo = entity.getTeamNo();
        if (TeamNo != null) {
            stmt.bindString(1, TeamNo);
        }
        stmt.bindLong(2, entity.getCompanyid());
 
        String Des = entity.getDes();
        if (Des != null) {
            stmt.bindString(3, Des);
        }
 
        Integer Total = entity.getTotal();
        if (Total != null) {
            stmt.bindLong(4, Total);
        }
 
        Integer State = entity.getState();
        if (State != null) {
            stmt.bindLong(5, State);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public TeamInfo readEntity(Cursor cursor, int offset) {
        TeamInfo entity = new TeamInfo( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // TeamNo
            cursor.getInt(offset + 1), // companyid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // Des
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // Total
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4) // State
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TeamInfo entity, int offset) {
        entity.setTeamNo(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setCompanyid(cursor.getInt(offset + 1));
        entity.setDes(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTotal(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setState(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(TeamInfo entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(TeamInfo entity) {
        return null;
    }

    @Override
    public boolean hasKey(TeamInfo entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
